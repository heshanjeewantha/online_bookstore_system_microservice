// Service paths — nginx proxies these to the real backend IPs at runtime
export const API_URLS = {
  USER_SERVICE:    "/api/users",
  BOOK_SERVICE:    "/api/books",
  ORDER_SERVICE:   "/api/orders",
  PAYMENT_SERVICE: "/api/payments",
};
